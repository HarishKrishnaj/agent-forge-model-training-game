{
  "name": "agent-forge-model-training-game",
  "version": "1.0.0",
  "description": "Playable educational AI model training game",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "serve": "python3 -m http.server 4173 --directory ."
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
